# Project 1 - Remote Backend Example

Example project using S3 remote backend.

## Prerequisites

Backend infrastructure must be created first (see `../create-backend/`)

## Usage

```bash
terraform init
terraform apply
```
