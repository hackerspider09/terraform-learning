# Project 2 - Remote Backend Example

Another example project using same S3 remote backend.

## Prerequisites

Backend infrastructure must be created first (see `../create-backend/`)

## Usage

```bash
terraform init
terraform apply
```
